#include <stdio.h>
#include <stdlib.h>
#include <Windows.h>
int main()
{
    system("del /S *.exe && del /S *.o");
    // system("git add .");
    // system("git commit -m\"add some code\"");
    // system("git push");
    return 0;
}